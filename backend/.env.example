# Backend Environment Variables Example

# MongoDB Connection String
# For local MongoDB: mongodb://localhost:27017/online_exam_db
# For MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/database_name
MONGODB_URI=your_mongodb_connection_string_here

# JWT Secret for authentication (change this to a random secret)
JWT_SECRET=your_jwt_secret_here_change_this

# Port for the server (optional, defaults to 5000)
PORT=5000

# Frontend URL for CORS (optional, defaults to http://localhost:3000)
FRONTEND_URL=http://localhost:3000